# Scripts

The CLI implementation will live here.

Planned files:

- `review-recorder.ps1` - PowerShell CLI for doctor, init, start, stop, brief, and analyze.
- `transcribe-whisper.py` - thin Python wrapper around faster-whisper.

Implementation will be added next.
